#include <stdio.h>

int main() {
    // Розрахунок періоду обертання Землі навколо Сонця
    double T = 365 + (1.0 / 4 + (1.0 / (7 + (1.0 / (1 + (1.0 / 3))))));

    // Виведення результату з плаваючою крапкою
    printf("T: %.10f day\n", T);

    return 0;
}
